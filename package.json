{
  "name": "scalejs",
  "version": "0.1.1",
  "description": "a simple module form scaling dom elements",
  "main": "src/scale.js",
  "repository": "/",
  "scripts": {
    "build-test": "gulp build && gulp build-test && webpack --config webpack.config.js",
    "build": "gulp build"
  },
  "author": "gottfried",
  "license": "MIT",
  "private": false,
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-concat": "^2.6.1",
    "gulp-sass": "^4.0.1",
    "webpack": "^4.16.0",
    "webpack-cli": "^3.0.8"
  }
}
